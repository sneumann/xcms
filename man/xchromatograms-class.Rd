% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DataClasses.R, R/functions-XChromatograms.R
\docType{class}
\name{XChromatograms-class}
\alias{XChromatograms-class}
\alias{XChromatograms}
\title{Data container for chromatographic data along with detected peaks}
\usage{
XChromatograms(data, phenoData, featureData, chromPeaks, ...)
}
\arguments{
\item{data}{For \code{XChromatograms}: a \code{list} of \link{Chromatogram} objects.}

\item{phenoData}{For \code{XChromatograms}: either a \code{data.frame},
\link{AnnotatedDataFrame} or \link{NAnnotatedDataFrame} describing the
phenotypical information of the samples.}

\item{featureData}{For \code{XChromatograms}: either a \code{data.frame} or
\link{AnnotatedDataFrame} with additional information for each row of
chromatograms.}

\item{chromPeaks}{For \code{XChromatograms}: a \code{list} of \code{matrix} objects with
required columns \code{"rt"}, \code{"rtmin"}, \code{"rtmax"}, \code{"into"}, \code{"maxo"} and
\code{"sn"} providing the peak definition and data. Length has to match the
length of \code{data}.}

\item{...}{For \code{XChromatograms}: optional parameters to be passed on to the
\code{matrix} function (such as \code{ncol} or \code{nrow} to define the numbber of
rows and colums of the resulting \code{XChromatograms} object.}
}
\value{
For \code{XChromatograms}: a \code{XChromatograms} object.
}
\description{
The \code{XChromatograms} class contains chromatographic data along with
identified peaks. The object inherits all functionality from the
\link{Chromatograms} object defined in the \code{MSnbase} package.

Object from the class can be created with the \code{XChromatograms} function
that takes arguments \code{data}, \code{phenoData}, \code{featureData} and \code{chromPeaks}.
}
\examples{

## Create some Chromatogram objects
c1 <- Chromatogram(rtime = c(1, 2, 3, 4), intensity = c(5, 3, 5, 7))
c2 <- Chromatogram(rtime = c(1, 2, 3, 4), intensity = c(7, 9, 0, 4))

}
\author{
Johannes Rainer
}
